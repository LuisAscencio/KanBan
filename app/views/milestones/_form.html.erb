<% if flash[:errors] %>
    <% flash[:errors].each do |error| %>
    <p><%= error %></p>
    <% end %>
<% end %>

<%= form_for(@milestone, class: "mx-auto") do |f| %>
<div class="form-group">
<%= f.text_field :title, :placeholder => "What do you want to accomplish?", :class => "form-control"%>
</div>


  <%
=begin%>
 <div class="form-group">
<%= f.label :category%>
<br>
<%= f.select :category, ["General", "Exercise", "Diet", "Career", "Other"], prompt: true %>
<%= f.hidden_field :completed?, :value => false %>
</div> 
<%
=end%>

<div class="input-group mb-3">
  <div class="input-group-prepend">
    <label class="input-group-text" for="inputGroupSelect01">Category</label>
  </div>
  <select value="milestone[category]" class="custom-select" id="inputGroupSelect01">
    <option selected>Choose...</option>
    <option value="general" name="general">General</option>
    <option value="exercise">Exercise</option>
    <option value="diet">Diet</option>
    <option value="career">Career</option>
    <option value="other">Other</option>
  </select>
</div>
<%
=begin%>
  
<%
=end%>
<div class="form-group">
<%= f.hidden_field :user_id, :value => current_user.id %>
<%= f.text_field :due_date, :placeholder => "When do you want to accomplish this goal?", :class => "form-control"%>
</div>
<div class="d-flex justify-content-between">
<%= f.submit :submit, :class => "btn btn-primary" %>
<% end %>   
<%= link_to  "Back to Milestones", root_path %>
</div>